services:
  - type: web
    name: betting-website
    env: node
    buildCommand: |
      npm install
      npm run generate
    startCommand: |
      cd .output/public && python3 -m http.server $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: $PORT